export type ImportedMetaData = {
    contract: string
    id?: string
    name?: string
    description?: string
    image?: string
}

export type SavedContracts = {
    tag?: string
    contract?: string
}
